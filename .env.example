# UUID (can be generated via `uuidgen`)
UUID=c39105bb-34cd-447a-bcb1-7e61d46b2a5d

# WebSocket path for Xray (must match nginx config)
WS_PATH=/ws_path

# Domain name (used for TLS and nginx server_name)
DOMAIN=localhost
